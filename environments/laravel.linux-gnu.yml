version: "3.5"
services:
  php-fpm:
    volumes:
      - ${SSH_AUTH_SOCK:-/dev/null}:/run/host-services/ssh-auth.sock

  php-debug:
    volumes:
      - ${SSH_AUTH_SOCK:-/dev/null}:/run/host-services/ssh-auth.sock
