#!/usr/bin/env bash
[[ ! ${WARDEN_DIR} ]] && >&2 echo -e "\033[31mThis script is not intended to be run directly!\033[0m" && exit 1

MAGENTO_HELP=$("${WARDEN_DIR}/bin/warden" cli "bin/magento")

WARDEN_USAGE=$(cat <<EOF
${MAGENTO_HELP}
EOF
)
